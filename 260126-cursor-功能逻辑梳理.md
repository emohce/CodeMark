# CodeRemarkTour 功能逻辑梳理（Baseline）

- **Baseline 时间**：2026-01-26
- **覆盖范围**：IntelliJ 平台插件 CodeRemarkTour（书签/流程/引用管理）
- **基线状态**：仅描述当前实现，无新增改动

## 目标
- 覆盖范围：IDE 侧书签/流程结构化管理、工具窗口交互、数据持久化与引用关系

## 功能边界
- 负责：在编辑器位置创建书签/分组/流程/说明节点；展示树形结构；引用关系维护；与 IntelliJ 内置书签同步；简易搜索与导航
- 不负责：远端同步、多用户协同、权限控制、复杂冲突解决

## 主流程
1. **书签创建入口**：动作在编辑器触发，收集位置与名称后构建 BookmarkNode，交由 ViewModel 处理
   - 动作声明与快捷键：`Add Bookmark Here` 等 @./src/main/resources/META-INF/plugin.xml#L52-L80
   - 书签动作实现：收集 caret 位置信息，调用 ViewModel 生成节点并选中 @./src/main/kotlin/emohce/presentation/action/CreateBookmarkAtCaretAction.kt#L17-L69
2. **DI & 服务组装**：ServiceLocator 按需构建仓库、用例与 ViewModel，供动作与 ToolWindow 复用 @./src/main/kotlin/emohce/core/di/ServiceLocator.kt#L13-L24
3. **工具窗口启动**：ToolWindowFactory 创建 ViewModel，挂载文档监听与导航监听，加载 BookmarkPanel UI @./src/main/kotlin/emohce/presentation/toolwindow/CodeRemarkTourToolWindowFactory.kt#L13-L35
4. **视图状态管理**：BookmarkViewModel 维护树状态、处理 Intent（创建/编辑/移动/删除/搜索/导航/引用）并下发 SideEffect（导航、通知、Inlay 刷新） @./src/main/kotlin/emohce/presentation/toolwindow/BookmarkViewModel.kt#L37-L544
5. **树形 UI 交互**：BookmarkPanel 订阅 state/side effect，支持增删改查、拖拽、搜索过滤、导航双击等；通过 SelectionBus 记忆选择 @./src/main/kotlin/emohce/presentation/toolwindow/panel/BookmarkPanel.kt#L79-L744
6. **持久化与撤销**：BookmarkStore 管理内存树与引用列表；读写 .bookmarkx/bookmarkx.json，保存前转换相对路径并生成撤销文件；支持 reload/undo @./src/main/kotlin/emohce/data/repository/BookmarkStore.kt#L15-L198
   - 底层数据源：文件序列化/反序列化、版本迁移、undo 文件写入 @./src/main/kotlin/emohce/data/datasource/BookmarkPersistentDataSource.kt#L13-L113
7. **领域仓库**：
   - BookmarkRepositoryImpl：树的 CRUD、搜索、移动/重排、事件流（SharedFlow）与节点级观察 @./src/main/kotlin/emohce/data/repository/BookmarkRepositoryImpl.kt#L13-L254
   - ReferenceRepositoryImpl：引用去重创建、计数、删除、循环检测 @./src/main/kotlin/emohce/data/repository/ReferenceRepositoryImpl.kt#L1-L56
8. **导航用例**：ProcessNavigationUseCase 在流程节点中定位前后书签与进度展示 @./src/main/kotlin/emohce/domain/usecase/navigation/ProcessNavigationUseCase.kt#L7-L67
9. **数据模型**：BookmarkNode 包含 Bookmark/Process/Group/Descriptive 四类，Process 支持嵌套与扁平化导航 @./src/main/kotlin/emohce/domain/model/BookmarkNode.kt#L6-L63

## 关键分支
- 创建节点时父容器缺省：默认挂载到 root @./src/main/kotlin/emohce/data/repository/BookmarkRepositoryImpl.kt#L150-L183
- 插入位置 index 为 null 或越界：追加到末尾 @./src/main/kotlin/emohce/data/repository/BookmarkRepositoryImpl.kt#L246-L253
- 路径转换失败或不在项目内：保持原绝对路径 @./src/main/kotlin/emohce/data/repository/BookmarkStore.kt#L22-L79
- 引用创建前循环检测，发现环则仅提示不落库 @./src/main/kotlin/emohce/presentation/toolwindow/BookmarkViewModel.kt#L379-L401
- 外部 bookmarkx.json 变更：VFS 监听触发 reload 与 Inlay 刷新 @./src/main/kotlin/emohce/presentation/toolwindow/BookmarkViewModel.kt#L211-L257

## 数据流
- **输入**：用户在编辑器/工具窗口的操作（动作、按钮、拖拽、搜索关键字）
- **状态**：BookmarkViewState（root 树、选中/展开节点、搜索结果、引用映射、进度）
- **输出**：
  - SideEffect：导航到文件、气泡通知、Inlay 刷新、选中/滚动到节点、刷新 bookmarkx.json
  - 持久化：.bookmarkx/bookmarkx.json 及 undo 文件
  - IntelliJ 内置书签同步（创建/更新/删除）@./src/main/kotlin/emohce/presentation/toolwindow/BookmarkViewModel.kt#L276-L376

## 依赖与影响
- 外部依赖：IntelliJ Platform（ToolWindow、Actions、VFS、Notifications、BookmarksManager）、kotlinx.serialization
- 影响范围：项目根目录下 `.bookmarkx` 数据文件；IDE 内置书签列表；编辑器 Inlay 与 gutter 图标显示

## 限制与注意
- 无远端同步或并发合并策略；撤销仅基于单文件副本
- 搜索为名称/描述包含匹配，limit 默认取 repository 实现（未在搜索 Intent 传入 limit 时使用默认值）
- Path 相对化依赖项目 basePath，外部路径保持绝对

## 代码关联（相对路径 + 行号）
- plugin 配置与 actions：@./src/main/resources/META-INF/plugin.xml#L1-L80
- ServiceLocator：@./src/main/kotlin/emohce/core/di/ServiceLocator.kt#L13-L24
- Bookmark 动作：@./src/main/kotlin/emohce/presentation/action/CreateBookmarkAtCaretAction.kt#L17-L69
- ToolWindow 入口：@./src/main/kotlin/emohce/presentation/toolwindow/CodeRemarkTourToolWindowFactory.kt#L13-L35
- ViewModel & Intent：@./src/main/kotlin/emohce/presentation/toolwindow/BookmarkViewModel.kt#L37-L544
- UI Panel（主要交互）：@./src/main/kotlin/emohce/presentation/toolwindow/panel/BookmarkPanel.kt#L79-L744
- 数据模型：@./src/main/kotlin/emohce/domain/model/BookmarkNode.kt#L6-L63
- 仓库实现：@./src/main/kotlin/emohce/data/repository/BookmarkRepositoryImpl.kt#L13-L254
- 引用仓库：@./src/main/kotlin/emohce/data/repository/ReferenceRepositoryImpl.kt#L1-L56
- 存储与持久化：@./src/main/kotlin/emohce/data/repository/BookmarkStore.kt#L15-L198
- 持久化数据源：@./src/main/kotlin/emohce/data/datasource/BookmarkPersistentDataSource.kt#L13-L113
- 导航用例：@./src/main/kotlin/emohce/domain/usecase/navigation/ProcessNavigationUseCase.kt#L7-L67

## 相对原始实现的累计修正说明
- 本文仅为现状梳理，未实施任何代码改动。

## 影响面与风险
- 文档整理行为不改变运行逻辑，无新增风险。

## 非目标
- 不涉及需求调整、接口变更、功能优化、样式修改、测试编写。

## 测试清单（空）
- 正常流程：N/A（未变更）
- 边界 / 空数据：N/A
- 权限 / 越权：N/A
- 并发 / 重复提交：N/A
- 回归影响点：N/A
