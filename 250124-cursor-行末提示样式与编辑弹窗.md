# 250124-cursor-行末提示样式与编辑弹窗.md

【范围声明】
仅用于已有代码的功能与逻辑关系梳理，不涉及任何开发或修改行为。

## 目标
- 覆盖范围：行末 inlay hints 的样式替换为自定义渲染（图标+文本+hover+click 编辑），并复现 Java 示例的完整交互行为。

## 功能边界
- 负责：替换 BookmarkLineEndInlayProvider 的 presentation 创建逻辑，新增自定义渲染器和编辑弹窗工具类，实现 hover 下拉图标、click 编辑弹窗、保存后刷新 hints。
- 不负责：不调整数据存储、repository 逻辑、快捷键绑定、UI 主题统一。

## 主流程
1. BookmarkLineEndInlayProvider 收集 hints 数据（节点标签、ID、文件路径、行号）。
2. 为每行调用 createBadgePresentation，生成自定义 InlayPresentation（图标 + 文本 + hover 状态 + click handler）。
3. 点击行末提示触发编辑弹窗（复用 BookmarkPanel 表单逻辑），保存后更新 repository 并刷新 inlay hints。

## 关键分支
- hover 状态：鼠标进入/离开 → 显示/隐藏下拉图标 + 切换光标为手型
- click 行为：查找节点 → 弹出编辑表单 → 保存时更新 repository → 触发 RefreshInlays 侧效

## 数据流（如有）
- 输入：nodeId, label, filePath, line, project → 编辑表单
- 状态：repository 更新 → 触发 ViewModel 刷新 → BookmarkPanel handleSideEffect
- 输出：hints 重新收集 → 渲染更新

## 依赖与影响
- 外部依赖：IntelliJ PresentationFactory (onHover, onClick, icon, seq), AllIcons, BookmarkPanel 编辑逻辑
- 影响范围：BookmarkLineEndInlayProvider, 新增 BookmarkInlayRenderer, BookmarkEditDialogUtil

## 限制与注意
- 已知限制：renderer 需线程安全，避免频繁重绘；弹窗生命周期与 inlay 耦合（编辑中 inlay 可能失效）
- 前置条件：Inlay Hints 设置启用

## 代码关联（必须可跳转，仅允许相对路径+行号范围）
- [code](./src/main/kotlin/emohce/presentation/editor/inlay/BookmarkLineEndInlayProvider.kt#120-123)
- [code](./src/main/kotlin/emohce/presentation/editor/inlay/BookmarkInlayRenderer.kt#1-50)
- [code](./src/main/kotlin/emohce/presentation/toolwindow/BookmarkEditDialogUtil.kt#1-30)
