# CodeRemarkTour 书签联动重构方案（高效简洁版）

- **目标**：消除内置书签与插件书签割裂，保证 gutter/工具窗口/JSON 持久化的一致性和左键联动。
- **范围**：书签创建/删除同步、gutter line marker 生成、工具窗口选中联动、日志与监听。
- **不做**：远端同步、多用户、权限、复杂冲突解决。

## 现状问题
1. 用户通过 IntelliJ 内置书签入口创建的标记只存在于 IDE 内置系统，未写入 `.bookmarkx/bookmarkx.json`，无插件 UUID 绑定 → gutter 左键无 SelectionBus 选中。
2. LineMarkerProvider 依赖插件存储生成 marker；内置书签未入库时 marker 缺失或不关联。
3. 日志不足，难以定位收集/点击链路。

## 重构思路
- **单一事实源**：插件自有存储（BookmarkStore）为唯一数据源；移除 IntelliJ 内置书签依赖，不再双写。所有展示与点击联动由自定义 gutter/inlay/树完成。
- **自定义渲染**：自定义 LineMarkerProvider + GutterIconRenderer（或在现有 provider 中自绘图标和 tooltip），不使用 IntelliJ 内置 BookmarksManager；样式、图标、颜色与 CodeTour/Bookmark-X 方案一致（纯插件侧控制）。
- **点击联动**：左键点击 gutter → 直接通过 SelectionBus 选中树节点并展开高亮，必要时导航到文件行；右键弹出插件自定义菜单（增/改/删/设为流程入口/引用）。
- **数据驱动**：标记生成仅读取 BookmarkStore；所有创建入口（工具栏按钮、编辑器动作、右键菜单）写入存储并保存 JSON，触发 reload/side effect。
- **日志与诊断**：收集、点击、自定义菜单操作均打印统一前缀日志，便于排查。
- **更激进方案（可选）**：
  - 完全绕过 LineMarkerProvider，使用 EditorMarkupModel 的 RangeHighlighter + custom GutterIconRenderer（与 CodeTour 的“虚拟书签”模式类似），可精确控制点击/hover/tooltip/右键菜单，无需 PSI 参与，适合大文件和懒加载。
  - 增加内存索引：预先构建 file→line→nodeId 映射，避免每次 collect 遍历整棵树；监听 BookmarkStore 变更增量更新索引。
  - 高亮与 inlay 一体：在同一 renderer 内提供 gutter 图标 + 行尾徽章 + 可选 inline text，减少多路 provider 的复杂度。
  - 右键菜单接入 ActionManager，以 ActionGroup 形式可复用现有 Create/Edit/Delete 逻辑，支持快捷键提示。
  - 提供“跳转并闪烁高亮”效果：点击后在编辑器应用短时范围高亮，提升可见度。
  - 快速刷新策略：监听 BookmarkStore/Reference 事件流，按文件粒度增量更新 RangeHighlighter，避免全量重绘。

### 更激进方案实现蓝图
1) 数据索引层
   - 启动时遍历 BookmarkStore，构建 `Map<filePath, TreeMap<line, nodeId>>`；存入 Project service，监听 repository 事件增量更新。
2) 标记渲染层（取代 LineMarkerProvider）
   - 打开文件时，按索引为每个命中行创建 RangeHighlighter；高亮只设置 gutter icon renderer（自定义类，提供左键/右键）。
   - 左键：SelectionBus.requestSelect → ToolWindow.show → 树选中；可选：在行范围 apply 背景闪烁。
   - 右键：ActionGroup 弹出菜单，复用现有 Create/Edit/Delete/SetEntry/AddReference 等逻辑。
3) 行尾徽章/inline 文本
   - 同一个 renderer 内绘制小徽章和可选文本（名称/序号），减少多 provider 并保证对齐。
4) 事件与刷新
   - repository 事件：按文件增量更新高亮（先清除该文件高亮，再重建）；
   - 文件关闭/重开：在 FileEditorManager listener 中重建对应高亮。
5) 性能与可用性
   - 避免 PSI，基于 Document/offset 工作，性能优于 LineMarkerProvider；
   - 索引 + 文件粒度刷新，适合大项目；
   - 兼容懒加载：仅对已打开文件建高亮。

### 合理性评估
- 优点：完全摆脱内置书签与 PSI，交互和样式 100% 自控，性能可控，左键联动最直接。
- 风险/成本：需手写 RangeHighlighter 生命周期管理、索引同步、ActionGroup 菜单；需要覆盖多编辑器实例切换、撤销/redo 后刷新。
- 可行性：参考 CodeTour/Bookmark-X 的“虚拟书签”实践，可实现；实现量中等，但收益（性能+可控性）高。

## 设计与实现要点
1) 去除 BookmarksManager 依赖
   - 删除 IntelliJBookmarkManager 的创建/更新/删除调用，不再反射创建内置书签。
   - 移除可能的 BookmarksManager listener/桥接逻辑，防止与内置系统互相影响。

2) 自定义 gutter 渲染与菜单（当前由 BookmarkHighlighterService 实现，2025-01-29 已移除 BookmarkLineMarkerProvider）
   - 在 BookmarkHighlighterService 的 RangeHighlighter 上设置 GutterIconRenderer，使用插件图标，覆盖 tooltip。
   - getClickAction / getPopupMenuActions：左键 SelectionBus.requestSelect(nodeId) + 打开 ToolWindow + 树选中；右键自定义菜单（编辑、删除、添加子节点等）。
   - 日志：`[GUTTER_HIGHLIGHT]`、`[GUTTER_CLICK]`。

3) 创建入口统一写入存储
   - 编辑器动作、工具栏按钮、右键菜单的“Add Bookmark/Group/Process/Note”全部调用 BookmarkRepository.create 后 save，触发 ViewModel reload → LineMarkerProvider 重绘。
   - 不再调用 IDE 内置 bookmark API。

4) 交互对齐 CodeTour / Bookmark-X 的方式
   - 树为唯一权威源；gutter 仅是视图。点击 gutter 等价于在树上选中该节点并自动滚动。
   - 支持懒加载和 selectNodeWithRetry，保证 reload 后也能选中。

5) 日志与诊断
   - `[GUTTER_COLLECT]` 收集、`[GUTTER_CLICK]` 左键、`[GUTTER_MENU]` 右键、`[REPO_CREATE]`/`[REPO_DELETE]` 按现有前缀。

6) 迁移
   - 启动时扫描 `.bookmarkx/bookmarkx.json` 生成标记；若本地有遗留的 IntelliJ 内置书签，可忽略（不再同步）。

## 关键接口与数据流
- 入口：内置书签新增/删除事件 → Bridge → BookmarkRepository → BookmarkStore.save → SelectionBus → BookmarkPanel.selectNodeWithRetry → 展开高亮。
- 标记：BookmarkHighlighterService 维护 fileIndex，绘制 gutter 图标（RangeHighlighter）→ 点击 → SelectionBus → 树选中。（原 BookmarkLineMarkerProvider 已删除。）

## 日志规划
- Bridge: `[BOOKMARK_BRIDGE] added file=... line=... uuid=... | skipped(reason)` / `removed ...` / `resolve failed`。
- LineMarker: `[GUTTER_COLLECT] begin/markers=...`，`[GUTTER_CLICK] handler.../click begin ...`。

## 迁移与兼容
- 旧内置书签：首次监听时可扫描内置组，缺少 `$uuid` 的条目自动补写入存储，生成 UUID，并更新描述为 `name$uuid`（可选增强）。
- 反向删除：删除插件节点时，保持现有 IntelliJBookmarkManager 同步删除。

## 风险与回滚
- 风险：监听误判导致重复节点；通过去重检查（uuid 或 file+line）降低风险。
- 回滚：Bridge 可开关（在服务构造中支持 disable flag），禁用后恢复现状。

## 开发步骤（推荐顺序）
1. 新建 IntelliJBookmarkBridge（Project Service）实现监听与入库/删库。
2. 在 ToolWindowFactory/ServiceLocator 初始化 bridge。
3. （可选）启动时对已有内置书签做一次同步补入库。
4. 验证日志链路与联动（见测试）。

## 测试要点
- 用 IDE 内置书签入口新增：应在树出现节点、.bookmarkx 写入、gutter 可左键联动选中。
- 用 IDE 内置书签删除：树节点与 gutter 同步消失。
- 插件动作新增/删除：行为不变，内置书签同步更新。
- 重启 IDE 后：桥接继续生效，旧内置书签如无 uuid 时能补全入库（若实现补全）。
- 日志：点击、收集、桥接均有预期日志。
