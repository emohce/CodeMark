# CodeRemarkTour 插件 - 操作入口与快捷键汇总

**基线**：2026-02-06，基于当前代码库静态梳理，供后续更新优化参考。

---

## 1. 操作入口（按位置）

### 1.1 主界面 - Tool Window

| 入口 | 说明 | 配置位置 |
|------|------|----------|
| **View → Tool Windows → CodeRemarkTour** | 打开/聚焦书签侧边栏 | `plugin.xml` → `<toolWindow id="CodeRemarkTour">` |
| 侧边栏图标 | 点击右侧边栏的插件图标打开 | 同上，`anchor="right"` |

- 实现：`CodeRemarkTourToolWindowFactory`，内容为 `BookmarkPanel`。

---

### 1.2 编辑器 - 右键菜单（EditorPopupMenu）

| 菜单项 | Action ID | 说明 |
|--------|-----------|------|
| Add CodeMark Here | `CodeRemarkTour.CreateBookmarkAtCaret` | 在光标处创建 CodeMark |
| Add Process Entry Here | `CodeRemarkTour.CreateProcessAtCaret` | 在光标处创建 Process 入口 |
| Add Group Here | `CodeRemarkTour.CreateGroupAtCaret` | 在当前选择下创建 Group |
| Add Note Here | `CodeRemarkTour.CreateNoteAtCaret` | 在当前选择下创建 Note |

- 配置：`plugin.xml` → `<actions>`，`<add-to-group group-id="EditorPopupMenu" anchor="last"/>`。
- 实现类：`CreateBookmarkAtCaretAction`、`CreateProcessAtCaretAction`、`CreateGroupAtCaretAction`、`CreateNoteAtCaretAction`（`presentation/action/`）。

---

### 1.3 编辑器 - Gutter（行号旁图标）

| 操作 | 说明 | 代码位置 |
|------|------|----------|
| **左键点击图标** | 在树中选中对应节点、显示 Tool Window、并高亮该行 | `BookmarkHighlighterService.kt` → `getClickAction()` "Select Bookmark" |
| **右键图标 → Edit** | 打开该节点编辑对话框 | `getPopupMenuActions()` → "Edit" |
| **右键图标 → Add Bookmark After This Node** | 在该节点后插入新 Bookmark | "Add Bookmark After This Node" |
| **右键图标 → Delete** | 删除该节点 | "Delete" |

- 实现：`BookmarkHighlighterService` 中 `GutterIconRenderer` 的 `getClickAction()` 与 `getPopupMenuActions()`。
- Gutter 图标由 `BookmarkLineEndPainter` 等与高亮服务配合绘制。

---

### 1.4 Tool Window 内 - 工具栏按钮

| 按钮 | 功能 | 对应方法（BookmarkPanel） |
|------|------|---------------------------|
| Add Group | 创建分组 | `createGroup()` |
| Add Process | 创建流程 | `createProcess()` |
| Add CodeMark | 创建书签 | `createBookmark()` |
| Add Note | 创建描述性笔记 | `createDescriptive()` |
| Add Step | 添加到流程步骤 | `addToProcessStep()` |
| Edit | 编辑选中节点 | `editSelected()` |
| Move | 移动选中节点 | `moveSelected()` |
| Delete | 删除选中节点 | `deleteSelected()` |
| Set Entry | 设置 Process 入口 | `setProcessEntry()` |
| Refresh | 刷新树 | `BookmarkIntent.Refresh` |
| Prev | 流程内上一项 | `BookmarkIntent.NavigateToPrevInProcess` |
| Next | 流程内下一项 | `BookmarkIntent.NavigateToNextInProcess` |
| Query + Search | 搜索 | `startSearch()` |
| Clear | 清除搜索 | `clearSearchAndRestore()` |
| Refs | 引用概览 | `showReferenceOverview()` |
| Graph | 引用图 | `showReferenceGraph()` |
| Keymap | 打开 IDE 快捷键设置 | `openKeymapSettings()` |
| B / P / N / G | 过滤：书签/流程/笔记/分组 | 筛选 + `startSearch()` |

- 实现：`BookmarkPanel.kt` 约 239–294 行，工具栏为 `JPanel` + `JButton`/`JToggleButton`。

---

### 1.5 Tool Window 内 - 树右键菜单（PopupMenu）

| 菜单项 | 功能 |
|--------|------|
| Add Group | 同工具栏 |
| Add CodeMark | 同工具栏 |
| Add Note | 同工具栏 |
| Edit | 同工具栏 |
| Move | 同工具栏 |
| Delete | 同工具栏 |
| Prev in Process | 流程内上一项 |
| Next in Process | 流程内下一项 |
| Refresh | 刷新 |

- 实现：`BookmarkPanel.createPopupMenu()`（约 1706–1741 行）。  
- 注释掉的菜单：Add Process、Set Process Entry、Reference 相关、Keymap Settings、Navigate 等。

---

### 1.6 Tool Window 内 - 树交互（无菜单）

| 操作 | 功能 |
|------|------|
| 树节点单击 | 选中并导航到书签/流程入口（单次左键即导航） |
| 树 Left / Right | 折叠/展开节点或移动选中（见下方快捷键） |
| 拖拽 | 通过 `NodeTransferHandler` 支持拖放调整结构 |

---

## 2. 快捷键汇总

### 2.1 全局/编辑器处注册（plugin.xml，可被 Keymap 修改）

| 快捷键 | Action ID | 说明 |
|--------|-----------|------|
| **Shift+F2** | `CodeRemarkTour.CreateBookmarkAtCaret` | 在光标处添加 CodeMark |
| **Shift+F3** | `CodeRemarkTour.CreateGroupAtCaret` | 添加 Group |
| **Shift+F4** | `CodeRemarkTour.CreateNoteAtCaret` | 添加 Note |

- `CreateProcessAtCaret` 未在 plugin.xml 中配置快捷键，仅通过右键菜单触发。
- 插件启动时会检查上述 Action 与系统 Keymap 冲突并提示（`checkShortcutConflicts()`）。

---

### 2.2 Tool Window 内 - 树获得焦点时

| 快捷键 | 功能 |
|--------|------|
| **Ctrl+G** | 创建 Group |
| **Alt+P** | 创建 Process |
| **Ctrl+B** | 创建 CodeMark |
| **Ctrl+N** | 创建 Note（描述性） |
| **Ctrl+E** | 编辑选中 |
| **Ctrl+M** | 移动选中 |
| **Ctrl+T** | 添加到 Process |
| **Ctrl+Shift+T** | 复制到 Process |
| **Ctrl+Shift+E** | 设置 Process 入口 |
| **Delete** | 删除选中 |
| **Ctrl+R** | 刷新 |
| **Ctrl+F** | 焦点移到搜索框 |
| **Alt+↑** | 流程内上一项 |
| **Alt+↓** | 流程内下一项 |
| **Enter** | 导航到选中书签/入口 |
| **Left** | 折叠节点或选中父节点 |
| **Right** | 展开节点或选中首子节点 |

- 实现：`BookmarkPanel.installShortcuts()`，通过 `bindShortcut(tree, ...)` 绑定到树的 `WHEN_ANCESTOR_OF_FOCUSED_COMPONENT`，仅在该 Tool Window 内、树或其子组件获得焦点时生效。

---

### 2.3 Tool Window 内 - 搜索框获得焦点时

| 快捷键 | 功能 |
|--------|------|
| **Escape** | 清除搜索并返回焦点到树 |
| **Ctrl+Enter** | 执行搜索 |

- 实现：同上，`bindShortcut(searchField, ...)`。

---

## 3. 代码索引（便于后续修改）

| 内容 | 文件路径（相对项目根） |
|------|------------------------|
| plugin 入口与 Action/快捷键声明 | `src/main/resources/META-INF/plugin.xml` |
| 编辑器右键 4 个 Action | `src/main/kotlin/emohce/presentation/action/*.kt` |
| Gutter 点击与右键菜单 | `src/main/kotlin/emohce/presentation/editor/highlighter/BookmarkHighlighterService.kt`（约 277–327 行） |
| Tool Window 工厂 | `src/main/kotlin/emohce/presentation/toolwindow/CodeRemarkTourToolWindowFactory.kt` |
| 工具栏、树右键、树/搜索框快捷键 | `src/main/kotlin/emohce/presentation/toolwindow/panel/BookmarkPanel.kt`（工具栏 ~239–294，右键 ~1706–1741，快捷键 ~1779–1812） |
| 快捷键冲突检查 | `BookmarkPanel.kt` → `checkShortcutConflicts()`（约 1751–1777） |

---

## 4. 后续可优化方向（建议）

1. **统一入口文档**：工具栏、树右键、Gutter 右键的文案与分组可对齐，便于用户记忆（如“添加书签/流程/分组/笔记”命名一致）。
2. **快捷键**  
   - 为 `CreateProcessAtCaret` 在 plugin.xml 中增加可选快捷键（或说明仅用菜单）。  
   - 树内快捷键与 IDE 常用键（如 Ctrl+F/Ctrl+R）可能冲突，可考虑改为 Alt+字母或集中在说明文档中，由用户按需在 Keymap 中调整。
3. **树右键菜单**：当前部分项已注释（Set Entry、Reference、Keymap 等），若功能保留可恢复并归类。
4. **国际化**：当前工具栏/菜单/ Gutter 多为英文硬编码，若需 i18n 可改为 `message()` 等并在此文档标注需翻译的 key。
5. **冲突检测**：`checkShortcutConflicts()` 仅包含 3 个 Action ID，若新增 plugin.xml 快捷键，可一并加入检测列表。

---

*文档为静态汇总，实际行为以运行时代码为准；修改 plugin.xml 或 Keymap 后需重启或重新加载 IDE 生效。*
