#   Fix gutter refresh 

* [ ] gutter should auto shows when idea starts
* [ ] gutter should refresh when bookmark is created/deleted
* [ ] left-click should auto open corresponding tree node, now only expand the tree on level 1, not the inner levels



